# E2E Test Session Configuration
# Required for automated E2E tests without manual OAuth flow

# Enable E2E session mint endpoint (backend)
E2E_SESSION_ENABLED=1

# Shared secret for HMAC signing (keep secure!)
# Generate with: openssl rand -hex 32
E2E_SESSION_HMAC_SECRET=your_secret_here

# E2E test user email (must exist in database)
E2E_USER=e2e@ledgermind.org
