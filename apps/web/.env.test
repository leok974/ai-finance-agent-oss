# E2E Test Environment Variables
# HMAC Authentication for /agent/* endpoints

# HMAC credentials (set these in CI secrets)
HMAC_CLIENT_ID=prod-ui
HMAC_SECRET=your-secret-here-replace-in-ci

# API paths
AGENT_PATH=/agent/chat
BASE_URL=https://app.ledger-mind.org

# Test configuration
PW_PROD_WORKERS=2
