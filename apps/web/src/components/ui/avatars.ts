// Default assistant avatar path. We bundle a tiny SVG in public/avatars.
// If the file is missing, components should gracefully fallback to a local SVG or initials.
export const ASSISTANT_AVATAR = "/avatars/robot.svg";
