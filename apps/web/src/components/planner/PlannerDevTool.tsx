import React from "react";
import Card from "@/components/Card";

export default function PlannerDevTool() {
  // Temporary implementation until real Planner tool is added to repo
  return (
    <Card className="p-3">
      <div className="font-medium mb-1">Planner DevTool (stub)</div>
      <div className="text-sm opacity-80">This is a stub for the Planner DevTool. Replace with the real component when available.</div>
    </Card>
  );
}
