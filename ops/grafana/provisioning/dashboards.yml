apiVersion: 1

providers:
  - name: 'LedgerMind ML'
    orgId: 1
    folder: 'LedgerMind'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards/ledgermind
