# E2E Testing Configuration Template
# Copy to .env.test and replace with actual values

# HMAC credentials (legacy naming from E2E session minting)
# Used for both UI tests (session minting) and API tests (HMAC auth)
E2E_USER=leoklemet.pa@gmail.com
E2E_SESSION_HMAC_SECRET=your-secret-here-replace-in-ci

# Alternative naming (also supported by HMAC utils)
# HMAC_CLIENT_ID=prod-ui
# HMAC_SECRET=your-secret-here

# Test environment
AGENT_PATH=/agent/chat
BASE_URL=https://app.ledger-mind.org
PW_PROD_WORKERS=2
PW_SKIP_WS=1  # Skip local dev server for prod tests
