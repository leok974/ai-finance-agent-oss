# Default base URL for Playwright and local scripts.
# Use nginx/prod preview port by default. If you run Vite dev, set 5173.
BASE_URL=http://127.0.0.1:8080

# Playwright workers (override concurrency)
PW_WORKERS=24
