# Core
POSTGRES_PASSWORD=changeme

# Backend dev
APP_ENV=dev
DEV_ALLOW_NO_AUTH=1
DEV_ALLOW_NO_LLM=1
ENCRYPTION_ENABLED=0         # set 1 + KMS vars if you want crypto locally
DATABASE_URL=postgresql+psycopg://myuser:$(POSTGRES_PASSWORD)@postgres:5432/finance
VITE_API_BASE=http://127.0.0.1:8000

# Optional LLM (host Ollama)
OPENAI_BASE_URL=http://host.docker.internal:11434/v1
OPENAI_API_KEY=ollama
LLM_MODEL=gpt-oss:20b

# Optional KMS (if enabling ENCRYPTION_ENABLED=1)
# GCP_KMS_KEY=projects/.../locations/us-east1/keyRings/.../cryptoKeys/...
# GCP_KMS_AAD=app=ledgermind,env=dev
# GOOGLE_APPLICATION_CREDENTIALS=/secrets/ledgermind-backend-sa.json
